VITE_SUPABASE_URL=https://odervrcycncwmfewrpkj.supabase.co
VITE_SUPABASE_PUBLISHABLE_DEFAULT_KEY=sb_publishable_gI9gCLxtg5d0c2RlU_2eYQ_Ng786FM8
VITE_SUPABASE_TABLE=uat

# Import script settings (optional)
# UAT_IMPORT_BATCH_SIZE=200
# UAT_IMPORT_MODE=insert # insert | upsert (upsert requires UNIQUE/PK on conflict column)
# UAT_IMPORT_CONFLICT=case_id
# UAT_TEST_STEPS_AS=array  # "array" (default) or "text" (join with newline)

# Recommended for imports (server-side only):
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

